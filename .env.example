# URL бэкенда (NestJS). Порт из backend/.env → PORT=3010.
VITE_API_URL=http://localhost:3010
# 1 — моки MSW (разработка без бэка). 0 — реальный бэкенд (нужен живой :3010 + npm run gen:api).
VITE_API_MOCK=0
