import { StudentDashboard } from "@/widgets/student-dashboard";

// Порт english-flow/src/routes/dashboard.tsx (composition only — FRONTEND.md §9).
export function DashboardPage() {
  return <StudentDashboard />;
}
